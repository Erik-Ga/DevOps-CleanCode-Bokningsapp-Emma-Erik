@page
@model BokningsAppDevOpsCleanCode.Pages.PersonalPageModel

<div>
    @if (Model.Bookings != null && Model.Bookings.Any())
    {
        <h1><u>Dina bokningar</u></h1>
        <div>
            @foreach (var booking in Model.Bookings)
            {
                <div class="bokningPersonalPage" style="border: 1px solid #ccc; padding: 10px; margin-bottom: 10px;">
                    <p>Datum: @booking.ChosenDateTime.ToString("yyyy-MM-dd")</p>
                    <p>Tid: @booking.ChosenTime</p>
                    <p>Behandling: @booking.ChosenTreatment</p>
                    <p>Namn: @booking.CustomerName</p>
                    <form method="post" asp-page-handler="CancelBooking" asp-route-bookingId="@booking.UserId">
                        <button class="avbokaPersonalPage" type="submit">Cancel Booking</button>
                    </form>
                </div>
            }
        </div>
    }
    else
    {
        <p>No bookings found.</p>
    }
</div>


